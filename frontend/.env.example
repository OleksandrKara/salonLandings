# Base URL of the backend API. In production this is same-origin via Nginx
# (proxied at /api), so it can be left empty. Set for local dev against a
# backend running on a different port.
VITE_API_BASE_URL=http://localhost:8000
